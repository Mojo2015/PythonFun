from tkinter import * #Imports all classes, attributes, and methods of tkinter into the current workspace
root = Tk()#Creates an instance of the class tkinter.Tk. This creates the root window (the basic blank window)
root.mainloop() #executes the event loop method of the root object. This is what keeps the window visible, if you remove this code
                #the window will close as soon as you open it.

"""
These 3 lines of code are used for every GUI. All information about your window will always go
between lines two and three, commit them to memory
"""
